<Window x:Class="SIMS_Projekat.SecretaryHome"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SIMS_Projekat"
        mc:Ignorable="d"
        Title="Secretary Home" Height="768" Width="1024"
        Closing="DataWindow_Closing" WindowStartupLocation="CenterScreen" WindowStyle="None">
    
    <Window.Resources>
        <Style TargetType="DataGridRow">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsUrgent}" Value="true">
                    <Setter Property="Background" Value="Red" />
                    <Setter Property="Foreground" Value="White" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ContentControl x:Name="Content" Grid.Column="1" Margin="10" Content="{Binding CurrentView}"/>

        <StackPanel Grid.Column="0">
            <Border Width="200" Height="768">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#76b852" Offset="0.0" />
                        <GradientStop Color="#8DC26F" Offset="1" />
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel>
                        <RadioButton Content="Nalozi" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="30" Height="50" Foreground="White"/>
                        <RadioButton Content="Termini" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="30" Height="50" Foreground="White"/>

                    </StackPanel>

                    <Label Grid.Row="1" Height="30" HorizontalAlignment="Center" FontSize="16" FontFamily="Arial" Foreground="DodgerBlue" Margin="0,0,0,20" MouseDown="LogOut_Click" Cursor="Hand">
                        <TextBlock TextDecorations="Underline">Izloguj se</TextBlock>
                    </Label>
                </Grid>
            </Border>

        </StackPanel>
    </Grid>

</Window>
